{
  "name": "@rnc/cli",
  "version": "0.0.0",
  "description": "⚙️ Plugin creator",
  "main": "lib/index.js",
  "bin": {
    "rainie-cli": "./lib/index.js"
  },
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.54",
    "@rnc/plugin-find": "^0.0.0",
    "chalk": "^1.1.3",
    "commander": "^2.9.0",
    "download-git-repo": "^1.0.2",
    "inquirer": "^3.0.6",
    "ora": "^2.1.0",
    "shelljs": "^0.8.2"
  }
}
