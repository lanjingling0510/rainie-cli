{
  "name": "@rnc/plugin-oss-upload",
  "version": "0.0.6",
  "description": "upload assets to oss",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0",
    "@rnc/oss": "^0.0.6",
    "@rnc/plugin-core": "^0.0.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
