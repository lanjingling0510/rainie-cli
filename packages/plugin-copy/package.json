{
  "name": "@rnc/plugin-copy",
  "version": "0.0.6",
  "description": "🔍 copy files to destination",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.56",
    "@rnc/plugin-core": "^0.0.4",
    "@rnc/shell": "^0.0.6",
    "@rnc/utils": "^0.0.5",
    "fs-extra": "^3.0.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
