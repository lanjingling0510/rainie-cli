{
  "name": "@rnc/npm",
  "version": "0.0.15",
  "description": "npm operation",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0",
    "@rnc/shell": "^0.0.20",
    "axios": "^0.18.0",
    "chalk": "^2.4.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
