{
  "name": "@rnc/shell",
  "version": "0.0.10",
  "description": "execute the bash command",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0",
    "@rnc/spinner": "^0.0.5",
    "chalk": "^2.4.1",
    "shelljs": "^0.8.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
