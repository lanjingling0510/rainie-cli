{
  "name": "@rnc/shell",
  "version": "0.0.4",
  "description": "execute the bash command",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.56",
    "@rnc/plugin-core": "^0.0.2",
    "chalk": "^1.1.3",
    "shelljs": "^0.8.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
