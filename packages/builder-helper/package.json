{
  "name": "@rnc/builder-helper",
  "version": "0.0.0",
  "description": "打包器帮助工具",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "react-dev-utils": "^5.0.1",
    "globby": "^4.1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
