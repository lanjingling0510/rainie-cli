{
  "name": "@rnc/logger",
  "version": "0.0.3",
  "description": "logger",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0",
    "@rnc/utils": "^0.0.13",
    "chalk": "^2.4.1",
    "log-symbols": "^2.2.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
