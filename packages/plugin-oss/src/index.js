import plugin from '@rnc/plugin-core';
import path from 'path';





export default (options) =>
  plugin('oss', async ({ files }) => {



    //
    // const copyPromises = files.map(async file => {
    //   await fs.copy(file.path, outDirRelative, { filter: filterFunc });
    // });

  });
