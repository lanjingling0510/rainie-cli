{
  "name": "@rnc/git",
  "version": "0.0.3",
  "description": "git operation",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "start": "npm run clean && gulp",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0",
    "@rnc/shell": "^0.0.7",
    "@rnc/utils": "^0.0.7",
    "chalk": "^1.1.3",
    "fs-extra": "^3.0.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
